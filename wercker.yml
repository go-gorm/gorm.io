box: node

build:
  steps:
    # A step that executes `npm install` command
    - npm-install
    # A step that executes `npm test` command
    - npm-test

    - script:
        name: hexo deploy
        code: |
          ls
          ls $WERCKER_CACHE_DIR/wercker/npm
          ./node_modules/hexo/bin/hexo deploy
